<?php
    // Create an array
    $numbers = array(1, 2, 3, 4, 5);

    // Reverse the array
    $reversed_numbers = array_reverse($numbers);

    // Print the reversed array
    foreach($reversed_numbers as $number) {
        echo $number . " ";
    }
?>
